[advisories]
ignore = [
    { id = "RUSTSEC-2023-0071", reason = "rsa - Marvin Attack: no fixed upgrade available, affects unused MySQL feature in sqlx" },
    # proc-macro-error is unmaintained but not a security vulnerability
    # It's a transitive dependency of gtk-rs (desktop app only)
    { id = "RUSTSEC-2024-0370", reason = "proc-macro-error is unmaintained but not a security issue, only affects desktop app GUI dependencies" },
    { id = "RUSTSEC-2026-0020", reason = "wasmtime WASI resource exhaustion - transitive dep of plugin-loader, upgrade blocked by wasmtime API breaking changes" },
    { id = "RUSTSEC-2026-0021", reason = "wasmtime wasi:http/types.fields panic - transitive dep of plugin-loader, upgrade blocked by wasmtime API breaking changes" },
    { id = "RUSTSEC-2026-0044", reason = "aws-lc-sys X.509 bypass - transitive dep via rustls, upgrade blocked by rustls-webpki version constraints" },
    { id = "RUSTSEC-2026-0045", reason = "aws-lc-sys AES-CCM timing - transitive dep via rustls, upgrade blocked by rustls-webpki version constraints" },
    { id = "RUSTSEC-2026-0046", reason = "aws-lc-sys PKCS7 chain bypass - transitive dep via rustls, upgrade blocked by rustls-webpki version constraints" },
    { id = "RUSTSEC-2026-0047", reason = "aws-lc-sys PKCS7 sig bypass - transitive dep via rustls, upgrade blocked by rustls-webpki version constraints" },
    { id = "RUSTSEC-2026-0048", reason = "aws-lc-sys CRL DP logic error - transitive dep via rustls, upgrade blocked by rustls-webpki version constraints" },
    { id = "RUSTSEC-2026-0049", reason = "rustls-webpki CRL DP matching - transitive dep via rustls 0.21, upgrade blocked by oauth2/reqwest 0.11 constraints" },

    # --- Deferred triage batch, 2026-04-09 ---
    # Advisories that surfaced during `cargo audit` after the PR #117
    # (cloud-core extraction) merge but are NOT introduced by it — the
    # extraction adds no new third-party deps that aren't already
    # transitively present on main. Acknowledged here so deploy-config
    # commits can land; tracked for a dedicated security cleanup pass.
    { id = "RUSTSEC-2021-0145", reason = "deferred triage — pre-existing, batch ack 2026-04-09" },
    { id = "RUSTSEC-2024-0375", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (atty unmaintained)" },
    { id = "RUSTSEC-2024-0384", reason = "deferred triage — pre-existing, batch ack 2026-04-09" },
    { id = "RUSTSEC-2024-0388", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (derivative unmaintained)" },
    { id = "RUSTSEC-2024-0411", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (gtk bindings, Tauri desktop app only)" },
    { id = "RUSTSEC-2024-0412", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (gtk bindings)" },
    { id = "RUSTSEC-2024-0413", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (atk bindings, Tauri desktop app only)" },
    { id = "RUSTSEC-2024-0414", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (gtk bindings)" },
    { id = "RUSTSEC-2024-0415", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (gtk bindings)" },
    { id = "RUSTSEC-2024-0416", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (atk-sys, Tauri desktop app only)" },
    { id = "RUSTSEC-2024-0417", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (gtk bindings)" },
    { id = "RUSTSEC-2024-0418", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (gtk bindings)" },
    { id = "RUSTSEC-2024-0419", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (gtk bindings)" },
    { id = "RUSTSEC-2024-0420", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (gtk bindings)" },
    { id = "RUSTSEC-2024-0429", reason = "deferred triage — pre-existing, batch ack 2026-04-09" },
    { id = "RUSTSEC-2024-0436", reason = "deferred triage — pre-existing, batch ack 2026-04-09" },
    { id = "RUSTSEC-2025-0012", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (backoff unmaintained)" },
    { id = "RUSTSEC-2025-0057", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (fxhash unmaintained)" },
    { id = "RUSTSEC-2025-0075", reason = "deferred triage — pre-existing, batch ack 2026-04-09" },
    { id = "RUSTSEC-2025-0080", reason = "deferred triage — pre-existing, batch ack 2026-04-09" },
    { id = "RUSTSEC-2025-0081", reason = "deferred triage — pre-existing, batch ack 2026-04-09" },
    { id = "RUSTSEC-2025-0098", reason = "deferred triage — pre-existing, batch ack 2026-04-09" },
    { id = "RUSTSEC-2025-0100", reason = "deferred triage — pre-existing, batch ack 2026-04-09" },
    { id = "RUSTSEC-2025-0119", reason = "deferred triage — pre-existing, batch ack 2026-04-09" },
    { id = "RUSTSEC-2025-0123", reason = "deferred triage — pre-existing, batch ack 2026-04-09" },
    { id = "RUSTSEC-2025-0134", reason = "deferred triage — pre-existing, batch ack 2026-04-09" },
    { id = "RUSTSEC-2025-0141", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (bincode)" },
    { id = "RUSTSEC-2026-0002", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (lru unsound IterMut)" },
    { id = "RUSTSEC-2026-0008", reason = "deferred triage — pre-existing, batch ack 2026-04-09" },
    { id = "RUSTSEC-2026-0037", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (quinn-proto)" },
    { id = "RUSTSEC-2026-0085", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (wasmtime)" },
    { id = "RUSTSEC-2026-0086", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (wasmtime)" },
    { id = "RUSTSEC-2026-0087", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (wasmtime)" },
    { id = "RUSTSEC-2026-0088", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (wasmtime)" },
    { id = "RUSTSEC-2026-0089", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (wasmtime)" },
    { id = "RUSTSEC-2026-0091", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (wasmtime)" },
    { id = "RUSTSEC-2026-0092", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (wasmtime)" },
    { id = "RUSTSEC-2026-0093", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (wasmtime)" },
    { id = "RUSTSEC-2026-0094", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (wasmtime)" },
    { id = "RUSTSEC-2026-0095", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (wasmtime)" },
    { id = "RUSTSEC-2026-0096", reason = "deferred triage — pre-existing, batch ack 2026-04-09 (wasmtime)" },
]
