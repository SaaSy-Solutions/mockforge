# Social Pack — Guide 01

## X/LinkedIn Thread (5–7 bullets)
1) Your UI is ready. Your backend isn’t. Enter Mockforge.  
2) Spin up a realistic API in minutes—no waiting on other teams.  
3) Deterministic data, dynamic templates, and chaos knobs (latency, faults).  
4) Use it locally, in CI, or as branch previews.  
5) Works with Playwright/Cypress/MSW.  
6) Demo repo + CLI in the first comment.  
7) Next: Routes 101 & Dynamic Responses.

## 3 Single-Post Options
- “Stop blocking on backends. Mockforge gives you a realistic API in minutes—deterministic data, chaos toggles, CI-ready.”
- “Design-first, test-fast. Mockforge is your programmable mock backend for UI + QA.”
- “Record. Replay. Iterate. Mockforge lets you simulate prod safely.”

## 20–30s Clip Beat Sheet
- 0–5s: `mockforge init` (terminal) → scaffold appears.
- 5–12s: `route add` → `/api/hello`
- 12–18s: `up` → curl result.
- 18–24s: UI route view + latency toggle.
- 24–30s: “Next: Routes 101” slate.
